# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build dummy-app-nx`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t dummy-app-nx`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system dummy-app-nx && \
          adduser --system -G dummy-app-nx dummy-app-nx

COPY dist/apps/dummy-app-nx dummy-app-nx
RUN chown -R dummy-app-nx:dummy-app-nx .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix dummy-app-nx --omit=dev -f install

CMD [ "node", "dummy-app-nx" ]
